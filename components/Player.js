export let Player = (name, score) => {
    return {
        name: name,
        score: score,
        // getSum : function() {
        //     let sum = 0 
        //     for (let i = 0; i < score.length; i++) {
        //         sum = sum + score[i]
        //     } 
        //     return sum
        // }
    }
}

